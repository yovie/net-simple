import { generatedBackendAppTypes } from './generated-backend-app-types';

describe('generatedBackendAppTypes', () => {
  it('should work', () => {
    expect(generatedBackendAppTypes()).toEqual('generated-backend-app-types');
  });
});
